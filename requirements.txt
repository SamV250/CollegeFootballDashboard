# Core dashboard + modeling stack. Pinned to lower bounds that are known
# to work; `pip install -r requirements.txt` in a fresh Python 3.11+ venv.
streamlit>=1.40,<2
pandas>=2.1
numpy>=1.26
plotly>=5.20
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
shap>=0.45
pyarrow>=15.0
PyYAML>=6.0
python-dotenv>=1.0
requests>=2.31

# --- development / CI ---
pytest>=8.0
ruff>=0.6

# macOS note: LightGBM / XGBoost need the OpenMP runtime:
#   brew install libomp
