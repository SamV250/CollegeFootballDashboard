# Bounded to the major lines the project is developed and tested against
# (see the tested versions in comments). Open-ended lower bounds caused
# the Docker image to resolve pandas 3 / numpy 2.4 / sklearn 1.9, which
# drift from what the tests run on.

streamlit>=1.40,<2          # tested 1.53
pandas>=2.1,<2.4            # tested 2.2.3
numpy>=1.26,<2.3           # tested 2.1.3
plotly>=5.20,<7            # tested 6.5.2
scikit-learn>=1.4,<1.7    # tested 1.5.2  (CalibratedClassifierCV cv='prefit' path)
lightgbm>=4.3,<5          # tested 4.7.0
xgboost>=2.0,<4          # tested 3.4.1
shap>=0.45,<0.55        # tested 0.52.0
pyarrow>=15.0
scipy>=1.11,<2
PyYAML>=6.0
python-dotenv>=1.0
requests>=2.31

# --- development / CI ---
pytest>=8.0
ruff>=0.6

# macOS note: LightGBM / XGBoost need the OpenMP runtime:
#   brew install libomp
# (the Docker image installs libgomp1 for the same reason)
